import styled from "styled-components";

export const TheStats = styled.span`
  position: absolute;
  right: 10px;
  top: 10px;
  color: white;
  padding: 5px 18px;
  background: rgba(0, 0, 0, 0.2);
  border-radius: 2px;
  font-size: 0.8rem;
`;
